namespace Ocuda.Utility.Keys
{
    public struct Cookie
    {
        /// <summary>
        /// Key for a unique identifier so this user can be identified when they return
        /// </summary>
        public static readonly string OpsAuthId = "ocuda.ops.auth.id";
    }
}