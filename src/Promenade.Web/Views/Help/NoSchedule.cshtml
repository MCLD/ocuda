@model Ocuda.Promenade.Models.Entities.SegmentText

@if (Model != null)
{
    if (!string.IsNullOrEmpty(Model.Header))
    {
        <h1>@Model.Header</h1>
    }
    @Html.Raw(Model.Text)
}
else
{
    <h1>@Localizer[Promenade.ScheduleNotAvailable]</h1>
}
