@model Ocuda.Ops.Controllers.Areas.Admin.ViewModels.Sections.DetailViewModel

<form asp-action="@Model.Action" method="post" role="form">
    <input asp-for="Section.Name" formgroup />
    <input asp-for="Section.Path" readonly="@(Model.IsReadonly)" formgroup />
    <input asp-for="Section.Icon" formgroup />
    <input asp-for="Section.SortOrder" formgroup />

    <div class="row">
        <div class="offset-md-3 col-md-9">
            <a class="btn btn-outline-secondary text-dark" asp-action="Index">Cancel</a>
            <button type="submit" class="btn btn-success btn-spinner" aria-label="Confirm">
                <span class="fa fa-save" aria-hidden="true"></span>
                Save
                <span class="fa fa-spinner fa-pulse fa-fw d-none"></span>
            </button>
        </div>
    </div>
</form>